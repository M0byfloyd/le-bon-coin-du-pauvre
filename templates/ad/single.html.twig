{% block body %}
    <div class="col-12 col-md-6 col-xl-3 m-3">
        <div class="card">
            <div class="card-body">
                <article>
                    <h5 class="card-title">{{ ad.title }}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">Votes : {{ ad.votes }}</h6>
                    <a class="btn btn-secondary" href="{{ path('lbcdp_ad_show',{slug: ad.slug}) }}">
                        Voir l'annonce
                    </a>
                </article>
            </div>
        </div>
    </div>
{% endblock %}